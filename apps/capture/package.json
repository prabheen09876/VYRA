{
  "name": "@vyra/capture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-wasm.mjs --dev && vite --host 0.0.0.0",
    "build": "vite build && node scripts/copy-wasm.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc -p tsconfig.json",
    "model:download": "node scripts/download-pose-model.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "1.0.1",
    "@vyra/core": "*"
  },
  "devDependencies": { "vite": "^7.1.5" }
}
