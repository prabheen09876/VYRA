# VYRA local pose server (YOLO26-pose + rep analyzers).
# Pinned loosely to the versions verified on Windows/Python 3.12.
ultralytics>=8.4.150
torch>=2.4
opencv-python>=4.9
numpy>=1.26
websockets>=12.0
